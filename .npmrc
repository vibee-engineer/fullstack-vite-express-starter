# React 19 hasn't landed in every peerDependency range yet (notably
# react-helmet-async). Use legacy-peer-deps so `npm install` completes
# without --force, matching the shadcn + Vite React 19 install story.
legacy-peer-deps=true
