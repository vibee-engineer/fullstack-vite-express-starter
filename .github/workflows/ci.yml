name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install
        run: npm install --workspaces --include-workspace-root

      - name: Generate Prisma client
        working-directory: server
        run: npx prisma generate

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Format check
        run: npm run lint

      - name: Audit (high and above)
        # Non-blocking: the two open advisories both need a breaking major bump
        # (see README "Known issues"). Kept visible so a NEW high never lands
        # unnoticed.
        continue-on-error: true
        run: npm audit --audit-level=high

      - name: Docker compose config
        run: docker compose config >/dev/null

      - name: Docker compose config (mongo overlay)
        run: docker compose -f docker-compose.yml -f docker-compose.mongo.yml config >/dev/null
