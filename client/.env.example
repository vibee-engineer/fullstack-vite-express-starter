# Base URL for axios. `/api` is nginx-relative — works in compose and any
# same-origin deploy. Override to an absolute URL (https://api.example.com)
# when deploying client + server separately.
VITE_API_URL=/api
