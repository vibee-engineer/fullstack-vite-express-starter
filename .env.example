# Root .env.example — placeholder.
# Real env lives in client/.env and server/.env (each has its own .env.example).
# Docker compose reads env directly from docker-compose.yml — no top-level .env
# needed for the default flow.
